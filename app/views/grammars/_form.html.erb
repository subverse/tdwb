<%= form_for(@grammar) do |f| %>
  <% if @grammar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grammar.errors.count, "error") %> prohibited this grammar from being saved:</h2>

      <ul>
      <% @grammar.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :info %><br />
    <%= f.text_area :info %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
